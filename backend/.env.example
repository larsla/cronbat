# Flask configuration
SECRET_KEY=your_secret_key_here
FLASK_DEBUG=true

# Server configuration
HOST=0.0.0.0
PORT=5000

# Database and logs configuration
CRONBAT_DB_PATH=instance/cronbat.db
CRONBAT_LOGS_PATH=instance/logs

# Execution history configuration
CRONBAT_MAX_EXECUTIONS=20
